# 🩺 الدليل المعماري والتقني الشامل لنظام "الحكيم الذكي" (Smart Healer System)

هذا الدليل مصمم لتقديم شرح احترافي، دقيق، ومفصل لكل قسم من أقسام النظام. يشرح الدليل رحلة المستخدم، آلية العمل التقنية، الخوارزميات، والأدوات المستخدمة في كل شاشة ليكون مرجعاً متكاملاً لأي شخص يريد فهم النظام بعمق.

---

## 1. قسم "تشخيص جديد" (New Diagnosis)
هذا القسم هو قلب النظام السريري، حيث يتم إدخال بيانات المريض للحصول على تشخيص فوري.

* **رحلة المستخدم (User Flow):**
  يقوم الطبيب بإدخال الأعراض النصية (مثل: "صداع نصفي مع غثيان")، ثم يضيف القراءات الحيوية (ضغط، نبض، حرارة، أكسجين). يمكنه أيضاً إرفاق صور طبية. عند الضغط على "تحليل الحالة"، يقوم النظام بعرض النتيجة في بطاقة تفاعلية توضح: اسم المرض، مستوى الخطورة (مستقرة، تدخل طبي، حرجة)، الأدوية المقترحة، والتوصيات.
* **كيف يعمل؟ (آلية التشخيص):**
  1. **تجميع البيانات (Data Aggregation):** يقوم النظام بجمع كل المدخلات (النصوص، الأرقام، الصور) وتغليفها برمجياً.
  2. **هندسة التلقين (Prompt Engineering):** تُرسل البيانات إلى محرك الذكاء الاصطناعي (Gemini) داخل قالب مصمم مسبقاً يوجه الذكاء الاصطناعي لتقمص دور "طبيب استشاري".
  3. **الاستدعاء المنظم (Structured JSON Output):** يُجبر النظام الذكاء الاصطناعي على عدم الرد بكلام عشوائي، بل بإرجاع البيانات في هيكل `JSON` صارم ومحدد (يحتوي على حقول: التشخيص، الأدوية، التوصيات، الخطورة).
* **الأدوات والتقنيات المستخدمة:**
  - `Google Gemini 1.5 Pro/Flash API` لمعالجة اللغة الطبيعية والمنطق الطبي.
  - `React State (useState)` لإدارة حالة النماذج والمدخلات الحية.
  - خوارزمية (Rule-based Engine) لتلوين واجهة النتيجة (أحمر للحالات الحرجة، أخضر للمستقرة) بناءً على مخرجات الـ JSON.

---

## 2. قسم "المستشعرات الحيوية" (Biosensors Hub)
قسم متطور لمحاكاة وقراءة المؤشرات الحيوية للمريض بدون الحاجة لأجهزة طبية ضخمة.

* **كيف يعمل؟ (مصادر البيانات):**
  - **قراءة الكاميرا (PPG - Photoplethysmography):** تقنية تستخدم كاميرا الهاتف الخلوي أو الحاسوب. عند وضع الإصبع على الكاميرا، تلتقط الكاميرا التغيرات الدقيقة جداً في لون البشرة الناتجة عن تدفق الدم مع كل نبضة قلب. يقوم النظام بحساب المسافة الزمنية بين هذه التدفقات لاستخراج معدل ضربات القلب (BPM) ونسبة الأكسجين (SpO2).
  - **قراءة منفذ الصوت (Audio / AUX):** يمكن توصيل مستشعرات تناظرية (Analog) بمنفذ السماعة. يقوم النظام بقراءة الترددات الصوتية وتحليلها.
  - **الإدخال اليدوي:** كخيار احتياطي لإدخال قراءات من أجهزة خارجية.
* **الخوارزميات والأدوات:**
  - `WebRTC & MediaStream API`: للوصول المباشر والآمن لكاميرا المستخدم.
  - `Web Audio API & AnalyserNode`: لإجراء "تحويل فورييه السريع" (FFT) لتحليل الترددات الصوتية.
  - **خوارزمية الإجماع الذكي (Smart Consensus):** في حال وجود تشويش في القراءة، يقوم النظام بعمل تصفية (Noise Filtering) ويعطي "نسبة ثقة" (Confidence Score). إذا كانت النسبة فوق 80% يتم اعتماد القراءة أوتوماتيكياً ونقلها لقسم "تشخيص جديد".

---

## 3. قسم "التحليل البصري" (Visual & Multimodal Analysis)
قسم مدمج مع التشخيص، مخصص لقراءة وفهم الصور الطبية (أشعة سينية، رنين مغناطيسي، صور جلدية).

* **كيف يعمل؟**
  يقوم الطبيب برفع صورة الأشعة أو التخطيط (ECG). يتم عرض معاينة للصورة في الواجهة.
  - **المعالجة المسبقة (Preprocessing):** يقوم المتصفح بضغط الصورة وتحويلها إلى نظام نصي `Base64` لضمان سرعة الإرسال وتقليل استهلاك البيانات.
  - **تحليل الرؤية (Vision Analysis):** تُرسل الصورة لنموذج الرؤية الحاسوبية (Gemini Vision). يعتمد النموذج على "الشبكات العصبية الالتفافية (CNNs)" التي تدربت على ملايين الصور الطبية.
  - يقوم النموذج باكتشاف الشذوذ (Anomalies) مثل الكسور، الأورام، أو التغيرات الجلدية، ويدمج هذه الملاحظات مع الأعراض النصية لإصدار تشخيص متكامل.

---

## 🎙️ 4. قسم "جلسات المتابعة والاستشارة المباشرة" (Smart Consultation & Follow-up)
يُمثل هذا القسم القفزة النوعية في النظام، حيث يتحول الذكاء الاصطناعي من مجرد أداة تحليل نصية إلى "استشاري طبي افتراضي" يعمل بالزمن الفعلي (Real-time AI Colleague). يتيح هذا القسم للطبيب إجراء مناقشة صوتية حية حول حالة المريض المعقدة لتطوير خطة العلاج ومتابعة التطورات.

* **كيف يعمل؟ (سير العمل المعرفي):**
  - **تفعيل الجلسة (Session Initialization):** يختار الطبيب ملف المريض من السجلات. يقوم النظام فوراً بفتح بيئة معزولة (Sandbox) مخصصة لهذا المريض فقط.
  - **الحقن السياقي المسبق (Pre-emptive Context Injection):** قبل أن يبدأ الطبيب بالتحدث، يقوم النظام برمجياً بتمرير ملف المريض كاملاً (العمر، الجنس، العلامات الحيوية، التشخيص المبدئي، والأدوية المقترحة) كـ "رسالة نظام" (System Instruction) خلف الكواليس. بفضل هذه الخطوة، يمتلك الذكاء الاصطناعي وعياً كاملاً بالسياق، فلا يحتاج الطبيب لإعادة سرد حالة المريض، بل يمكنه البدء فوراً بسؤال مثل: "هل ترى أن استبدال الدواء كذا بدواء كذا مناسب لهذا المريض بالنظر لضغط دمه؟"
  - **المحادثة الثنائية المباشرة (Full-Duplex Communication):** يتبادل الطبيب والذكاء الاصطناعي النقاش عبر قنوات اتصال ثنائية الاتجاه دون الحاجة لانتظار تحميل أو كتابة نصوص.

* **الخوارزميات والأدوات التقنية المستخدمة:**
  - **بروتوكول WebSockets & Gemini Multimodal Live API:** للتواصل اللحظي (Low-Latency). يتم تجاوز بروتوكول HTTP التقليدي لإنشاء نفق اتصال مستمر (Persistent Connection) يتدفق عبره الصوت ذهاباً وإياباً في أجزاء من الثانية.
  - **مُعالجة الصوت المتقدمة (Web Audio API & PCM Encoding):**
    - **الاستقبال (Input):** يستخدم النظام `MediaDevices.getUserMedia` لالتقاط صوت الطبيب، ثم يقوم بتجزئة الإشارة الصوتية (Audio Chunks) وتشفيرها بصيغة (Base64 PCM) بتردد 16kHz لإرسالها بشكل متدفق (Streaming) للذكاء الاصطناعي.
    - **التشغيل (Playback):** عند وصول رد الذكاء الاصطناعي، يتم فك تشفير البيانات الصوتية (Decoded Audio Buffer) وتشغيلها باستخدام `AudioContext` بتردد 24kHz لضمان جودة صوت نقية، طبيعية، وخالية من التقطيع التكنولوجي (Jitter-free).
  - **نظام التراجع والتعطيل الآمن (Graceful Degradation):** في حال ضعف الإنترنت وعدم قدرة الـ Live API على العمل، يمتلك النظام خوارزمية احتياطية (Fallback) للتبديل إلى توليد النصوص وقراءتها عبر محرك التحدث (Web Speech API / TTS) المدمج في المتصفح لضمان عدم توقف الاستشارة أبداً.

* **ما يميز هذا القسم؟**
  - **الذكاء السريري التفاعلي:** الذكاء الاصطناعي هنا ليس روبوتاً للدردشة العامة، بل مبرمج بـ (Persona) "طبيب استشاري خبير"، يجيب بمهنية عالية، يقدم اقتراحات لتقليل الأعراض الجانبية، ويشارك في عصف ذهني طبي (Medical Brainstorming) لدعم القرار النهائي للطبيب في الحالات المعقدة، ويعمل كـ "رأي طبي ثانٍ" (Second Opinion) في أي وقت وفي أي مكان.

---

## 5. قسم "السجلات الطبية" (Patient Records)
نظام إدارة البيانات والملفات (EMR) المصمم ليكون فائق السرعة ومضاداً لانقطاع الإنترنت.

* **كيف يعمل؟**
  يعرض هذا القسم قائمة بكل المرضى السابقين مع خاصية البحث الفوري والفلترة السريعة (بحسب الحالة: حرجة، مستقرة).
* **خوارزمية المزامنة الهجينة (Offline-First Two-Way Sync):**
  - **محلياً (IndexedDB via Dexie.js):** عند إضافة مريض، يتم حفظه *فوراً* في قاعدة بيانات المتصفح المحلية. هذا يجعل عملية البحث والفتح تتم في 0 ميلي ثانية، ويضمن عمل النظام حتى في الصحراء بدون إنترنت.
  - **سحابياً (Supabase - PostgreSQL):** في الخلفية، يتأكد النظام من وجود إنترنت. إذا وُجد، يقوم بصمت بإرسال السجلات الجديدة للسحابة، ويجلب أي سجلات جديدة من أجهزة أخرى. إذا لم يوجد إنترنت، لا يظهر أي خطأ يزعج المستخدم، بل يتم تأجيل المزامنة حتى يعود الاتصال.

---

## 6. قسم "التحليل الذكي" (Smart Analytics & Power BI)
لوحة قيادة تفاعلية متقدمة (Advanced Dashboard) تعمل كمركز استخبارات الأعمال (BI) للمستشفى أو العيادة، مصممة لاستخراج رؤى طبية دقيقة من البيانات المتراكمة.

* **كيف يعمل؟ (دورة حياة البيانات):**
  - **استخلاص البيانات (Data Extraction):** يقوم النظام بجمع كافة السجلات الطبية المخزنة محلياً (آلاف السجلات) في أجزاء من الثانية.
  - **المعالجة والفرز (Data Processing):** تُمرر البيانات عبر خوارزميات تجميع (Aggregation) متطورة لفرزها حسب معايير متعددة (العمر، الجنس، مستوى الخطورة، نوع التشخيص، والأعراض الشائعة).
  - **التمثيل البصري (Data Visualization):** تترجم البيانات المعالجة إلى رسوم بيانية تفاعلية (مخططات دائرية، أشرطة، ومخططات خطية للتوجهات الزمنية).

* **الخوارزميات والأدوات التقنية المستخدمة:**
  - **خوارزميات التجميع الزمني والمنطقي (Map/Reduce & Time-series Aggregation):** يقوم النظام ببرمجة حلقات تكرارية عالية الأداء تمر على البيانات لحساب معدلات انتشار حالات معينة في فترات زمنية محددة (مثل زيادة حالات ضيق التنفس في فصل الشتاء).
  - **مكتبة Recharts (SVG/Canvas):** بدلاً من جداول البيانات المعقدة والمملة، يستخدم النظام مكتبة رسوميات متقدمة تعتمد على تفاعل المستخدم (Hovering/Tooltips) لعرض التفاصيل فوراً دون إعادة تحميل الصفحة.
  - **التوليد المدعوم بالذكاء الاصطناعي (AI-Driven Insights):** لا يكتفي القسم بعرض الأرقام فحسب، بل يمكن ربط هذه الإحصائيات بمحرك Gemini AI لتقديم "ملخص تنفيذي" (Executive Summary) يستنتج مثلاً: "لوحظت زيادة بنسبة 15% في الحالات الحرجة لكبار السن هذا الأسبوع، يُنصح بتجهيز أسرة عناية مركزة إضافية."

* **ما يميز هذا القسم؟**
  يحول هذا القسم البيانات الخام المتناثرة إلى **قرارات قابلة للتنفيذ (Actionable Insights)**. فهو يساعد مدير المستشفى على توزيع الكوادر الطبية بناءً على كثافة الحالات، ويساعد الأطباء في رصد الأوبئة أو التوجهات المرضية (Trends) بشكل مبكر واحترافي، كل ذلك في واجهة مظلمة أنيقة (Dark Mode/Dashboard) تحاكي منصات Power BI الاحترافية.

---

## 7. قسم "الإعدادات" (Settings & Architecture)
غرفة التحكم التقنية الخاصة بالنظام.

* **كيف يعمل؟**
  - يتيح إدخال مفاتيح الذكاء الاصطناعي (API Keys) بأمان، حيث يتم حفظها محلياً في `localStorage` ولا تُرسل لأي خوادم وسيطة.
  - يتيح إدارة قاعدة البيانات (أخذ نسخة احتياطية محلية بصيغة JSON، أو استعادة نسخة سابقة).
  - يحتوي على أدوات لمسح السجلات وإعادة ضبط النظام بالكامل.

## 🌟 ما يميز هذا النظام (The Ultimate Edge):
ما يجعل "الحكيم الذكي" نظاماً استثنائياً يتفوق على الأنظمة المتواجدة في السوق:
1. **اللامركزية والسرعة (Edge Computing):** النظام يعالج الكثير من العمليات (مثل ضغط الصور وقراءة المستشعرات) على جهاز المستخدم مباشرة، مما يجعله فائق السرعة ولا يحتاج لخوادم ضخمة.
2. **يعمل بدون إنترنت (Offline-Resilience):** بفضل معمارية IndexedDB + Dexie.js، النظام لا ينكسر أو يتعطل عند فقدان الاتصال، بل يعمل بكامل طاقته الأساسية ويزامن البيانات لاحقاً.
3. **ذكاء اصطناعي "أصيل" (Native AI):** الذكاء الاصطناعي هنا ليس مجرد "نافذة دردشة" منفصلة، بل هو مدمج كـ "محرك" يقرأ الأرقام الحيوية، ينظر لصور الأشعة، ويسمع صوت الطبيب ليتخذ قرارات متكاملة.
4. **واجهة مستخدم إنسانية (Humane UI):** يعتمد على Tailwind CSS و Framer Motion لتقديم واجهة ناعمة، مريحة للعين (بدون جداول بيانات مملة)، وحركات فيزيائية سلسة تقلل الإرهاق البصري للأطباء خلال مناوباتهم الطويلة.
